// need to import a helper librarys to get content on the screen
import 'package:flutter/material.dart';
import 'src/app.dart';

// define a 'main' function to run when app starts
void main() {
	runApp(App());
}
